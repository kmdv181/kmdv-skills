{
  "name": "ghostty-config",
  "version": "0.1.5",
  "description": "Edit your Ghostty terminal config by talking to Claude. Locates the config the same way Ghostty does, validates every change with `ghostty +validate-config` before it touches the real file, and keeps timestamped backups you can roll back. Install it on the machine Ghostty runs on.",
  "author": {
    "name": "kmdv",
    "email": "i@kmdv.dev"
  },
  "license": "MIT",
  "keywords": [
    "ghostty",
    "terminal",
    "config",
    "dotfiles",
    "keybinds",
    "themes"
  ]
}
